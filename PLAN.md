# DIXON — план развития и версии

Сайт + бэкенд сервисного центра Samsung (Чебоксары, Калинина 91к1).
Запуск: `start.bat` → http://127.0.0.1:8099/ · Админка: `/admin.html` (токен `ADMIN_TOKEN`).

## Как версионируем

- Каждый запрос к ассистенту = новая версия: коммит + git-тег.
- Схема: `v1.0`, `v1.1`, `v1.2`… за каждую доработку; `v2.0`, `v3.0` — за крупные вехи.
- Перед коммитом: проверка в браузере (консоль чистая, ключевые сценарии работают).
- Откат: `git checkout vX.Y`. Данные заявок (`data/`) в git не входят.

## История версий

- **v1.0** — базовый релиз: редизайн лендинга, прайс с поиском, квиз, калькулятор, FAQ,
  форма + трекер заявок, бэкенд (API, админка, CSV, статистика), PWA, a11y, печать.
- **v1.1** — автономность РФ: шрифты локально (24 woff2), GSAP удалён, ноль внешних
  запросов; гарантийный талон с печатью; дата обновления цен; /favicon.ico с сервера.
- **v1.2** — баланс hero: цифры вынесены в полосу на всю ширину (дыры справа больше
  нет); бургер-меню включается с 1150px — шапка в одну строку везде.
- **v1.3** — баланс bento: в большую карточку «Экран» добавлены популярные цены
  и ссылка на подбор по модели — пустоты снизу нет.

## Бэклог (по приоритету)

1. Гарантийный талон: печать из админки по заявке.
2. Фото поломки: загрузка в форме + просмотр в админке.
3. Прайс из админки: редактирование цен без правки кода.
4. Telegram-уведомления: подключить бота (нужен токен от владельца).
5. Автозапуск сервера при включении ПК (Task Scheduler) + инструкция.
6. Ротация бэкапов заявок (хранить 7 дней).
7. Яндекс.Метрика (нужен ID счётчика).
