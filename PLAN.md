# DIXON — большой план развития

Сайт + бэкенд сервисного центра Samsung (Чебоксары, Калинина 91к1).
Локально: `start.bat` → http://127.0.0.1:8099/ · Админка: `/admin.html` (токен `ADMIN_TOKEN`).
Публично: https://kigihara.github.io/Site-Dixon/ (статика, GitHub Pages, бесплатно).
Репозиторий: https://github.com/Kigihara/Site-Dixon · Текущая версия: **v1.28**.

## 1. Куда идём

Три цели: (а) сайт-визитка, который приводит звонки; (б) приём заявок с сайта
(дальше всё живёт в 1С:Предприятии — заказы, склад и касса там, дублировать
не будем); (в) свой хостинг в РФ, чтобы всё работало без VPN у клиентов
и без зарубежных сервисов.

## 1.1. Безопасность (v1.6, сделано)

- Трекер статусов удалён (статусы — в 1С); inbox заявок, «Готово» и CSV для
  переноса в 1С — оставлены.
- IP больше не хранится в заявках (в согласии обещаны только имя и телефон).
- Антибрутфорс админки: 10 неверных токенов → блокировка IP на 10 минут.
- Токен только в заголовке `X-Admin-Token` (в URL — лишь для SSE, иначе
  EventSource не умеет).
- Строгий CSP без `unsafe-inline`: свои файлы + хеши инлайн-скриптов/стилей,
  считаются сервером при старте. Инлайн-стили и обработчики вычищены.
- Security-заголовки: nosniff, SAMEORIGIN, no-referrer.
- Сервер предупреждает в логе, если запущен с дефолтным `ADMIN_TOKEN`.
- Осталось на переезд (VPS): сменить токен через env, HTTPS (Let's Encrypt),
  пин Docker-образа по digest.

**Срочно вручную:** отозвать GitHub-токен из истории чата после окончания
работ с хостингом; задать сильный `ADMIN_TOKEN` на боевом сервере.

## 2. Корпоративный менеджер realtime

Принцип: одна кодовая база, ноль зависимостей, данные — у себя.

- **Фаза A — живая лента (готово, v1.4).** SSE-шина `/api/events`: новая заявка
  мгновенно всплывает тостом в открытой админке, статусы обновляются сами.
- **Фазы B–D — ОТМЕНЕНЫ: заказы, склад и касса уже в 1С.** Не дублируем:
  сайт отдаёт заявки (inbox + CSV для переноса в 1С), статусы живут в 1С.
  Из корпоративного остались: тосты о новых заявках, CSV, статистика.

## 3. Хостинг и сервисы для РФ (проверено, сентябрь 2026)

Честный итог: зарубежные PaaS для продакшена в РФ — плохая идея. IP Vercel
частично блокируются, трафик Cloudflare режут с июня 2025, хостеры вне реестра
РКН под ограничениями, оплата российской картой не проходит. У Railway разовый
кредит $5 (~месяц), у Render free засыпание через 15 минут и эфемерный диск
(`leads.json` там не выживает — нужен Postgres).

Что уже сделано:
1. **GitHub Pages — ВКЛЮЧЁН (бесплатно).** Статика (лендинг, прайс, FAQ)
   живёт на https://kigihara.github.io/Site-Dixon/, деплой — автопуш в `main`
   через Actions. В РФ открывается. Формы там показывают честную подсказку
   про сервер.
2. **Render — готов конфиг** (`render.yaml`, free tier): нужен аккаунт
   (регистрация пользователем, ~5 минут) + подключить репозиторий. Для тестов.
3. **Продакшен — RU VPS** (Timeweb / Aeza / Beget / VDSina, ~250–400 ₽/мес,
   оплата МИРом): `package.json` + `Dockerfile` в репо, диск постоянный,
   realtime работает. Домен `dixon-sc.ru` — A-запись на VPS, SSL Let's Encrypt.
4. Сервисы: почта — на регистраторе домена; метрика — Яндекс.Метрика (нужен ID);
   карты/отзывы — Яндекс + 2ГИС (встроены); аптайм — UptimeRobot (free).
5. Фолбэк: ПК в сервисе + `start.bat` + автозапуск через планировщик.

Важно: репозиторий пришлось сделать публичным — иначе Pages не включается.
Секретов в коде нет (дефолтный токен меняйте через `ADMIN_TOKEN`).

## 4. Дорожная карта версий

- **v1.8** — фирменный синий вместо лайма; экран телефона наполнен статусами;
  чекбоксы починены; шапка-компакт с 1600px.
- **v1.9** — админка 2.0: шапка-пульт с онлайном и выходом, свитч live,
  пилюли статусов, кликабельные телефоны, относительное время, редактор цен
  в панели.
- **v2.0** — Telegram-бот для персонала, Яндекс.Метрика, автозапуск на ПК,
  фото поломки, ротация бэкапов, переезд на VPS + домен + HTTPS.

Старый бэклог: фото, прайс-редактор — в плане выше; гарантийный талон — готов.

## 5. Как версионируем

- Каждый запрос = новая версия: коммит + git-тег (`v1.5`, `v1.6`…; `v2.0` — вехи).
- Перед коммитом: проверка в браузере (консоль чистая, сценарии работают).
- Push в `main` + тег. Откат: `git checkout vX.Y`. Данные (`data/`) в git не входят.

## 6. История версий

- **v1.0** — базовый релиз: редизайн лендинга, прайс с поиском, квиз, калькулятор, FAQ,
  форма + трекер заявок, бэкенд (API, админка, CSV, статистика), PWA, a11y, печать.
- **v1.1** — автономность РФ: шрифты локально (24 woff2), GSAP удалён, ноль внешних
  запросов; гарантийный талон с печатью; дата обновления цен; /favicon.ico с сервера.
- **v1.2** — баланс hero: цифры в полосу на всю ширину; бургер с 1150px.
- **v1.3** — баланс bento: популярные цены в карточке «Экран».
- **v1.4** — realtime: SSE-шина, тосты о новых заявках; кнопки merged в «Общие»;
  `package.json` + `Dockerfile` для деплоя.
- **v1.5** — хостинг: относительные пути (живёт на любом подпути), GitHub Pages
  включён, `render.yaml`, спек realtime-системы с ролями.
- **v1.6** — безопасность + фокус на 1С: трекер удалён (статусы в 1С), без IP
  в заявках, антибрутфорс (10 попыток → бан 10 мин), токен только в заголовке,
  строгий CSP без unsafe-inline, варнинг о дефолт-токене.
- **v1.7** — цены из админки, CORS, тримминг токена.
- **v1.8** — синий ребренд, экран телефона, чекбоксы, шапка.
- **v1.9** — админка 2.0: пульт с онлайном, свитч live, пилюли, tel-ссылки, выход.
- **v1.10** — шапка: статус только с 1700px, запрет переносов в навигации.
- **v1.11** — визуал C (гибрид): SVG-иконки везде вместо эмодзи, aurora hero, glass lab с tilt, premium hover bento, stagger reveals, опциональные CDN-шрифты с локальным фолбэком, CSP allowlist fonts.googleapis/gstatic.
- **v1.12** — плавный возврат lab-tilt: ease-out lerp через rAF 450мс вместо резкого сброса, класс tilt не снимается, `will-change` только в наклоне.
- **v1.13** — прайс glow-up: липкая шапка в скролл-боксе, клик по строке → калькулятор, подсветка совпадений, счётчик, empty-state; починка поиска после подгрузки цен с бэкенда.
- **v1.14** — `AGENTS.md`: локальная инструкция для ассистента (git, сервисы, ограничения, проверки); дополнять каждый чат.
- **v1.15** — шапка: активный пункт меню больше не заливается синим — белый текст + тонкая черта-индикатор.
- **v1.16** — зерно `.grain`: opacity .07 → .035, фактура осталась, но вдвое тише.
- **v1.17** — мобильный премиум: свайп-таблица с липкой колонкой модели, умный callbar (прячется на контактах, safe-area), тапы 44px+, без серой вспышки тапа.
- **v1.18** — hero 2.0: полноэкранный на десктопе, живой mesh-градиент, крупный заголовок с градиентом, хинт скролла.
- **v1.19** — hero в один экран: убраны 100svh и хинт (вываливали статистику за вьюпорт), уплотнены отступы, заголовок 76px, lab без min-height.
- **v1.20** — FAQ в две колонки: вопросы во всю ширину + липкая CTA-карточка «Не нашли свой случай», пустоты справа больше нет.
- **v1.21** — телефон в hero длиннее: экран 320→360 + третья строка «Выдача», темноты внутри и под ним меньше.
- **v1.22** — квиз и калькулятор в языке прайса: светлая карточка формы, тёмная CTA-панель, подписанные поля, акцентный вывод цены.
- **v1.23** — вывод калькулятора без коробки: открытая компоновка, ценник 28px, ссылка с подчёркиванием.
- **v1.24** — пустое состояние калькулятора: приглушённый хинт с тире-заглушкой по центру, класс `is-empty`.
- **v1.25** — убрана выдуманная очередь: плашка в hero, её JS и фейковый `/api/status`; часы работы и «Открыто» (по реальному времени) оставлены.
- **v1.26** — часы до 19:00 везде, красный индикатор когда закрыто, из футера убраны концепт-подпись и TCL; новые услуги: калибровка АКБ и сканера (по 2500 ₽), официальная разблокировка 5 брендов.
- **v1.27** — мультибренд: Huawei/Honor/Tecno/Infinix/Realme/Oppo в бегущей строке, в «Неисправностях» и в мета-описании (hero не вырос).
- **v1.28** — админка 2.1 + TG-бот: статус «Принята» (Новая→Принята→Готово), фильтр-сегмент, SVG-иконки, «Бланк» убран; бот шлёт заявки с кнопкой «Принять», подтверждение синкается в админку по SSE (long-poll, токен только в env).
