# DIXON — большой план развития

Сайт + бэкенд сервисного центра Samsung (Чебоксары, Калинина 91к1).
Локально: `start.bat` → http://127.0.0.1:8099/ · Админка: `/admin.html` (токен `ADMIN_TOKEN`).
Публично: https://kigihara.github.io/Site-Dixon/ (статика, GitHub Pages, бесплатно).
Репозиторий: https://github.com/Kigihara/Site-Dixon · Текущая версия: **v1.5**.

## 1. Куда идём

Три цели: (а) сайт-визитка, который приводит звонки; (б) корпоративный менеджер
реального времени для приёмки, мастеров и кассы; (в) свой хостинг в РФ, чтобы всё
работало без VPN у клиентов и без зарубежных сервисов.

## 2. Корпоративный менеджер realtime

Принцип: одна кодовая база, ноль зависимостей, данные — у себя.

- **Фаза A — живая лента (готово, v1.4).** SSE-шина `/api/events`: новая заявка
  мгновенно всплывает тостом в открытой админке, статусы обновляются сами.
- **Фаза B — роли и смены (v1.6–v1.7).** Пользователи в `data/users.json`:
  директор (всё), менеджер (заявки, касса), мастер (только свои заказы).
  Вход по личному токену, страница `/team.html`. Онлайн-присутствие: heartbeat
  по SSE — видно, кто сейчас на смене.
- **Фаза C — заказ-наряды (v1.8–v1.9).** У заявки: статусы
  `новый → диагностика → в работе → готов → выдан`, исполнитель, устройство,
  состав работ, сумма. Комментарии к заказу летят всем по SSE. Назначение
  мастеру одной кнопкой, push в Telegram мастеру.
- **Фаза D — склад и касса (v2.0).** Запчасти: приход/расход, остатки,
  автосписание на заказ, порог «заканчивается». Касса за день, средний чек,
  печать талона/чека/акта.
- **Фаза E — клиенты и масштаб.** Telegram-бот (крючок уже в коде, нужен токен),
  SMS (SMS.RU/SmsAero), онлайн-запись со слотами, второй филиал, аналитика
  (воронка, сроки, загрузка мастеров).

## 3. Хостинг и сервисы для РФ (проверено, сентябрь 2026)

Честный итог: зарубежные PaaS для продакшена в РФ — плохая идея. IP Vercel
частично блокируются, трафик Cloudflare режут с июня 2025, хостеры вне реестра
РКН под ограничениями, оплата российской картой не проходит. У Railway разовый
кредит $5 (~месяц), у Render free засыпание через 15 минут и эфемерный диск
(`leads.json` там не выживает — нужен Postgres).

Что уже сделано:
1. **GitHub Pages — ВКЛЮЧЁН (бесплатно).** Статика (лендинг, прайс, FAQ)
   живёт на https://kigihara.github.io/Site-Dixon/, деплой — автопуш в `main`
   через Actions. В РФ открывается. Формы там показывают честную подсказку
   про сервер.
2. **Render — готов конфиг** (`render.yaml`, free tier): нужен аккаунт
   (регистрация пользователем, ~5 минут) + подключить репозиторий. Для тестов.
3. **Продакшен — RU VPS** (Timeweb / Aeza / Beget / VDSina, ~250–400 ₽/мес,
   оплата МИРом): `package.json` + `Dockerfile` в репо, диск постоянный,
   realtime работает. Домен `dixon-sc.ru` — A-запись на VPS, SSL Let's Encrypt.
4. Сервисы: почта — на регистраторе домена; метрика — Яндекс.Метрика (нужен ID);
   карты/отзывы — Яндекс + 2ГИС (встроены); аптайм — UptimeRobot (free).
5. Фолбэк: ПК в сервисе + `start.bat` + автозапуск через планировщик.

Важно: репозиторий пришлось сделать публичным — иначе Pages не включается.
Секретов в коде нет (дефолтный токен меняйте через `ADMIN_TOKEN`).

## 4. Дорожная карта версий

- **v1.6** — роли и `/team.html`: вход по токену, свои заказы, присутствие.
- **v1.7** — заказ-наряды: статусы, исполнитель, устройство, сумма, комментарии.
- **v1.8** — склад + касса + печать чека/акта.
- **v1.9** — Telegram-бот для клиентов + SMS (нужны токены/провайдер).
- **v2.0** — переезд на VPS + домен + HTTPS (инструкция внутри релиза).

Старый бэклог (актуален): фото поломки, прайс из админки, ротация бэкапов,
Яндекс.Метрика, автозапуск на ПК.

## 5. Как версионируем

- Каждый запрос = новая версия: коммит + git-тег (`v1.5`, `v1.6`…; `v2.0` — вехи).
- Перед коммитом: проверка в браузере (консоль чистая, сценарии работают).
- Push в `main` + тег. Откат: `git checkout vX.Y`. Данные (`data/`) в git не входят.

## 6. История версий

- **v1.0** — базовый релиз: редизайн лендинга, прайс с поиском, квиз, калькулятор, FAQ,
  форма + трекер заявок, бэкенд (API, админка, CSV, статистика), PWA, a11y, печать.
- **v1.1** — автономность РФ: шрифты локально (24 woff2), GSAP удалён, ноль внешних
  запросов; гарантийный талон с печатью; дата обновления цен; /favicon.ico с сервера.
- **v1.2** — баланс hero: цифры в полосу на всю ширину; бургер с 1150px.
- **v1.3** — баланс bento: популярные цены в карточке «Экран».
- **v1.4** — realtime: SSE-шина, тосты о новых заявках; кнопки merged в «Общие»;
  `package.json` + `Dockerfile` для деплоя.
- **v1.5** — хостинг: относительные пути (живёт на любом подпути), GitHub Pages
  включён, `render.yaml`, спек realtime-системы с ролями.
