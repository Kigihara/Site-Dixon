# DIXON — большой план развития

Сайт + бэкенд сервисного центра Samsung (Чебоксары, Калинина 91к1).
Запуск локально: `start.bat` → http://127.0.0.1:8099/ · Админка: `/admin.html` (токен `ADMIN_TOKEN`).
Репозиторий: https://github.com/Kigihara/Site-Dixon · Текущая версия: **v1.4**.

## 1. Куда идём

Три цели: (а) сайт-визитка, который приводит звонки; (б) корпоративный менеджер
реального времени для приёмки, мастеров и кассы; (в) свой хостинг в РФ, чтобы всё
работало без VPN у клиентов и без зарубежных сервисов.

## 2. Корпоративный менеджер realtime

Принцип: одна кодовая база, ноль зависимостей, данные — у себя.

- **Фаза A — живая лента (готово, v1.4).** SSE-шина `/api/events`: новая заявка
  мгновенно всплывает тостом в открытой админке, статусы обновляются сами.
- **Фаза B — заказ-наряды (следующая).** У заявки появляются статусы
  `новый → диагностика → в работе → готов → выдан`, исполнитель, устройство
  (модель/IMEI), состав работ и сумма. Склад запчастей: приход/расход,
  остатки, списание на заказ. Касса за день + отчёт. Печать: талон (есть),
  чек, акт выполненных работ.
- **Фаза C — люди и клиенты.** Роли: приёмщик / мастер / кассир / владелец,
  у каждого свой токен и права. Журнал действий (кто что поменял).
  Уведомления клиенту: Telegram-бот (крючок уже в коде, нужен токен) и SMS
  (нужен провайдер РФ: SMS.RU / SmsAero). Онлайн-запись со слотами времени.
- **Фаза D — масштаб.** Второй филиал, сводная аналитика (воронка, средний чек,
  сроки ремонта, загрузка мастеров), открытое API для сайта и мини-приложения.

## 3. Хостинг и сервисы для РФ (проверено, сентябрь 2026)

Честный итог исследований: зарубежные PaaS для продакшена в РФ — плохая идея.
IP Vercel частично блокируются (сайты не открываются без VPN), трафик
Cloudflare режут с июня 2025, хостеры вне реестра РКН под ограничениями,
оплата российской картой не проходит. Бесплатные тарифы Render/Railway/Fly —
для тестов: Render free засыпает через 15 минут простоя, у Railway разовый
кредит $5 (~месяц), у всех эфемерный диск (наш `leads.json` там не выживет —
нужен Postgres, т.е. переписывать хранилище).

Рекомендация:
1. **Продакшен — RU VPS** (Timeweb / Aeza / Beget / VDSina, ~250–400 ₽/мес,
   оплата МИРом). Наш код едет как есть: `package.json` + `Dockerfile` уже
   в репозитории, диск постоянный, realtime работает. Домен `dixon-sc.ru` —
   A-запись на VPS, SSL — Let's Encrypt, автозапуск — systemd.
2. **Тесты/превью — Render free** (засыпает, данные тестовые) или Railway
   на разовом кредите.
3. **Статика (запасной вариант)** — GitHub Pages из этого репозитория, но
   бэкенд всё равно нужен, поэтому проще всё на VPS.
4. **Сервисы:** почта — на текущем регистраторе домена; метрика —
   Яндекс.Метрика (нужен ID); карты/отзывы — Яндекс + 2ГИС (уже встроены);
   аптайм-мониторинг — UptimeRobot (free).
5. **Фолбэк:** действующий ПК в сервисе + `start.bat` + автозапуск через
   планировщик — работает уже сейчас, бесплатно.

## 4. Дорожная карта версий

- **v1.5** — заказ-наряды: статусы, исполнитель, устройство, сумма.
- **v1.6** — склад запчастей + списание на заказ.
- **v1.7** — роли и журнал действий; печать чека/акта.
- **v1.8** — Telegram-бот для клиентов + SMS (нужны токены/провайдер).
- **v1.9** — онлайн-запись со слотами.
- **v2.0** — переезд на VPS + домен + HTTPS (инструкция внутри релиза).

Старый бэклог (актуален): фото поломки, прайс из админки, ротация бэкапов,
Яндекс.Метрика, автозапуск на ПК.

## 5. Как версионируем

- Каждый запрос = новая версия: коммит + git-тег (`v1.4`, `v1.5`…; `v2.0` — вехи).
- Перед коммитом: проверка в браузере (консоль чистая, сценарии работают).
- Push в `main` + тег. Откат: `git checkout vX.Y`. Данные (`data/`) в git не входят.

## 6. История версий

- **v1.0** — базовый релиз: редизайн лендинга, прайс с поиском, квиз, калькулятор, FAQ,
  форма + трекер заявок, бэкенд (API, админка, CSV, статистика), PWA, a11y, печать.
- **v1.1** — автономность РФ: шрифты локально (24 woff2), GSAP удалён, ноль внешних
  запросов; гарантийный талон с печатью; дата обновления цен; /favicon.ico с сервера.
- **v1.2** — баланс hero: цифры вынесены в полосу на всю ширину; бургер с 1150px.
- **v1.3** — баланс bento: популярные цены в карточке «Экран».
- **v1.4** — realtime: SSE-шина, тосты о новых заявках в админке; кнопки merged
  в «Общие проблемы»; `package.json` + `Dockerfile` для деплоя.
