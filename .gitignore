# Playwright CLI output (may contain credentials)
.playwright-cli/
# Agent skills (tooling, not project code)
.claude/
# Local secrets (NEVER commit: Supabase/TG tokens live here)
.env
# Backend runtime data (personal data!)
data/
*.log
